{
    "name": "ironmcp/core",
    "description": "MCP servers that refuse unknown arguments instead of silently dropping them — advertisement == runtime.",
    "type": "library",
    "license": "MIT",
    "keywords": ["mcp", "model-context-protocol", "conformance", "hardening", "json-schema", "additionalProperties", "laravel", "wordpress", "woocommerce"],
    "require": {
        "php": "^8.1",
        "mcp/sdk": "^0.8"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "autoload": {
        "psr-4": { "IronMcp\\": "src/" }
    },
    "autoload-dev": {
        "psr-4": { "IronMcp\\Tests\\": "tests/" }
    },
    "config": {
        "allow-plugins": true,
        "sort-packages": true
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
